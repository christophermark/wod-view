source "https://rubygems.org"

gem "fastlane", "~> 2.237.0"
# expo prebuild shells out to `pod` from within `bundle exec fastlane`; the
# binstub resolves through Bundler, so CocoaPods must be in the bundle.
gem "cocoapods", "~> 1.17"
